[@react.component]
let make = () =>
  <div className={CssHelpers.tcss("mt-16 text-xs text-gray-700")}>
    {React.string(
       "Please note that all content on ShareCalculation.com is user generated, and we take no responsibility for anything. Really, nothing, so send your lawyers elsewhere.",
     )}
  </div>;